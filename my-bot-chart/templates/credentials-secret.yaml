apiVersion: v1
kind: Secret
metadata:
  name: telegram-bot-credentials
  labels:
    {{- include "my-bot-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  TELEGRAM_TOKEN: {{ .Files.Get "telegram-token.txt" | quote }}
  credentials.json: |
    {{ .Files.Get "credentials.json" | indent 4 }}
