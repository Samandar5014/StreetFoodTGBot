apiVersion: v1
kind: Secret
metadata:
  name: {{ include "my-bot-chart.fullname" . }}-secret
  labels:
    {{- include "my-bot-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  TELEGRAM_TOKEN: {{ .Values.secrets.TELEGRAM_TOKEN | quote }}
  credentials.json: |
{{ .Files.Get "credentials.json" | indent 4 }}
